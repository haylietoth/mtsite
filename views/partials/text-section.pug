section.text-content.scroll-transition-fade
  span
    != PrismicDOM.RichText.asText(project.title) + " "
    i.material-icons arrow_forward
  != " " + PrismicDOM.RichText.asText(slice.primary.text, ctx.linkResolver)
