<nav id="mobile-menu" class="fixed top-0 left-0 w-full bg-white border-b border-gray-100 z-[55] px-8">
    <div class="flex items-center justify-center">
        <% partials.data.navigation.map(navLink => { %>
          <a class="text-style hover:opacity-50 transition-opacity" href="<%- navLink.link.url %>" target="<%- navLink.link.target %>">
            <%- navLink.label %>
          </a>
        <% }) %>
    </div>
</nav>