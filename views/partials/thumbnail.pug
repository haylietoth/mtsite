div.thumbnail.no_space.formatted
 a(href=PrismicDOM.Link.url(slice.primary.link, ctx.linkResolver))
  div.thumb_image
   img(name='image-link', src=slice.primary.image.url, onmouseover=this.src=slice.primary.hover_image.url, onmouseout=this.src=slice.primary.image.url)